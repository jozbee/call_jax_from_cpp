@AGENTS.md

When asked for latency numbers, run `tools/rt_check.sh`, read `/proc/loadavg`,
and include both in the answer — a number without them cannot be interpreted
later, and a number taken under load is invalid rather than noisy.
