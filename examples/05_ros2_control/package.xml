<?xml version="1.0"?>
<package format="3">
  <name>jax_arm_controller</name>
  <version>0.1.0</version>
  <description>
    A ros2_control position controller that calls an ahead-of-time exported
    JAX function through pjrt::Function.  Example 05 of call_jax_from_cpp.
  </description>
  <maintainer email="noreply@example.com">call_jax_from_cpp</maintainer>
  <license>Unlicense</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>controller_interface</depend>
  <depend>hardware_interface</depend>
  <depend>pluginlib</depend>
  <depend>rclcpp</depend>
  <depend>rclcpp_lifecycle</depend>

  <exec_depend>controller_manager</exec_depend>
  <!-- Publishes /joint_states, which is how a run shows that the arm moved. -->
  <exec_depend>joint_state_broadcaster</exec_depend>
  <exec_depend>robot_state_publisher</exec_depend>
  <exec_depend>launch</exec_depend>
  <exec_depend>launch_ros</exec_depend>
  <exec_depend>ament_index_python</exec_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
